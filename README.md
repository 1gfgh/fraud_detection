# Fraud Detection

## Описание проекта

Данный научно-исследовательский проект (НИР, 3 курс) посвящён задаче постфактум-детекции фродовых пользователей на основе пакетов транзакций.  
Цель работы — исследовать и сравнить несколько подходов к моделированию транзакционных данных разной степени структурированности и оценить их вклад в качество выявления мошеннических пользователей.  
В рамках проекта предполагается построение моделей, которые по истории транзакций определяют пользователей с наибольшей вероятностью фродовой активности.

## Команда проекта
- Афоничев Артемий Олегович
- Бердов Игорь Вячеславович
- Долгих Даниил Денисович

## Используемые данные

В качестве исходных данных используется синтетический датасет **PaySim**, доступный на Kaggle:

🔗 https://www.kaggle.com/datasets/ealaxi/paysim1

Выбор данного датасета обусловлен тем, что реальные банковские транзакции недоступны для open-source исследований, а PaySim является наиболее популярным и широко используемым синтетическим датасетом с разметкой фродовых транзакций.

Датасет имитирует реальные финансовые операции и содержит:
- информацию о транзакциях между пользователями;
- типы операций;
- временную структуру;
- бинарную разметку мошеннических транзакций.

Подробное описание структуры данных и процесса их генерации приведено на странице датасета.

## Архитектура решения

Проект состоит из трёх логически независимых, но сравнимых между собой блоков. Каждый блок реализует собственный подход к обработке транзакционных данных и обучению модели.

### 1. Базовая модель на «чистых» батчах транзакций

В данном блоке транзакции рассматриваются как табличные данные без сложной структуризации.

### 2. Модель с полноценным структурированием транзакций

В рамках данного подхода поток транзакций преобразуется в структурированное представление графовыми методами.

### 3. Промежуточная модель для выделения паттернов

Данный блок реализует двухэтапный подход:
1. модель, выявляющая локальные или временные паттерны фродового поведения;
2. модель верхнего уровня, обучающаяся на выходах первой модели.

Подход можно рассматривать как:
- автоматическое извлечение паттернов;
- генерацию мета-фичей;
- последующий стекинг или ансамблирование моделей.
